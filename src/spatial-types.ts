export type SpatialWorld={regional?:{destinationWeights?:number[];terrainImage?:string;dataParts?:{file:string;sha256:string;foodCells:number;access:number}[];initialSites?:number[];origin:number[];edges:number[][];foodCells:number[][];access:number[][];foragingRadiusKm:number;resourceQuadratureKm:number;water:string};land?:number[];rainfall?:(number|null)[];temperature?:(number|null)[];environment?:{name:string;place:string;yearsBP:number;seaLevelM:number;sourceManifest:string;staticDuringRun:boolean};width:number;cellKm:number;extent:number;center:number[];elevation:(number|null)[];sites:number[][];fertility:number[];female:number[];male:number[];ecology:{referencePeople:(number|null)[];referenceLow:(number|null)[];referenceHigh:(number|null)[];areaKm2:number[];knownAreaKm2:number[];densityRaster:(number|null)[];mobilityPairs:number[][];fit:{n:number;blockedCvLogRmse:number;baselineLogRmse:number}}};
export type SpatialGroup={currentResidenceYears?:number|null;arrivalPrediction?:{year:number;predicted:number;actual:number;error:number}|null;establishedSite?:boolean;condition?:{deficit:number;residentPersonDays:number;travelPersonDays:number};id:number;people:number;from:number[];to:number[];departure:number;arrival:number;site:number;reason:string;cost:number;shortfall:number;provisions:number;knowledge?:{observedSites:number;destinationObservationAgeDays:number|null};food?:{carriedKcal:number;reserveDays:number;consumedKcal:number;unmetKcal:number;lastUnmetKcalDay:number;localCoverage:number}};
export type FoodFrame={foragingKcal?:number;seasonality:number;renewalMultiplier:number;initialKcal:number;renewedKcal:number;stockKcal:number;carriedKcal:number;consumedKcal:number;spoiledKcal:number;unmetKcal:number;stocksKcal:number[]|Record<number,number>;balanced:boolean;policy:string};
export type SpatialFrame={outcomes?:{residentPersonDays:number;travelPersonDays:number;establishedSites:number;viableOccupiedSites:number;recolonizations:number;outerBoundaryAttempts:number;splits:number;merges:number};coupled?:boolean;scarcityDeaths?:number;food?:FoodFrame;year:number;people:number;births:number;deaths:number;blockedEdges:number;groups:SpatialGroup[];trips:SpatialTrip[];mobility:{id:number;referenceMovesYear:number;referenceKmYear:number;scheduledKm:number;departures:number}[]};
export type SpatialSettings={initialFraction?:number;response?:{lagDays:number;maxHazardYear:number;threshold:number};coupled?:boolean;seed:number;density:string;mode:'reference'|'food';foodSupply:number;seasonality?:number;knowledge?:'perfect'|'learned'};

export type SpatialTrip={reason:string;id:number;site:number;from:number[];to:number[];departure:number;arrival:number};
