type Row={year:number;people:number;births:number;deaths:number;cumulative_births:number;cumulative_deaths:number;expected_people:number};
type Data={with_births:Row[];without_births:Row[];metadata:{fitted_total_fertility_scale:number};checks:{replicates:number}};
let cached:Data|undefined;
export async function mountGrowth(root:HTMLElement){
 root.innerHTML='<p role="status">Loading the demographic reference…</p>';
 try{
  if(!cached){const response=await fetch('/reference/growth.json');if(!response.ok)throw Error('The demographic run could not load. Select this view again to retry.');cached=await response.json();}
  if(root.hidden||document.querySelector('[data-lab="growth"]')?.getAttribute('aria-pressed')!=='true')return;
  const data=cached!;let year=80,births=true;
  root.innerHTML=`<div class="reference-note"><strong>Modern demographic reference</strong><p>Recorded Rust runs · births, deaths and aging · no migration or resource limits.</p></div><div class="reference-layout"><div class="window"><div class="window-title">Population over 160 years</div><div class="growth-controls"><label for="growth-comparison">Compare<select id="growth-comparison"><option value="births">Births and deaths</option><option value="closed">Deaths only</option></select></label><p>Same starting population: 5,000 people, distributed across ages. Pink shows the seeded run; dashed black shows its mathematical expectation.</p></div><div class="reference-figure"><svg id="growth-chart" viewBox="0 0 800 390" role="img" aria-label="Population through time compared with expected population"></svg></div><div class="reference-timeline"><label for="growth-year">Elapsed time <output id="growth-year-label"></output></label><input type="range" id="growth-year" min="0" max="160" value="80"><div class="reference-scale"><span>0 years</span><span>160 years</span></div></div></div><aside class="window reference-inspector"><div class="window-title">Population accounting</div><div id="growth-readout" aria-live="polite"></div><div id="reference-evidence" class="reference-evidence"><h2>How growth was calibrated</h2><p>The modern !Kung reference uses an age-specific fertility shape digitized from a published figure. Its scale is fitted to the published 0.26% annual intrinsic growth reference.</p><p>That fit implies a fertility scale of <strong>${data.metadata.fitted_total_fertility_scale.toFixed(2)}</strong> births per woman under this model’s timing assumptions. This is a fitted value, not a new measurement.</p><details><summary>Evidence and limitations</summary><p>Fertility: <a href="https://doi.org/10.1371/journal.pone.0000785" target="_blank" rel="noopener">Tuljapurkar et al., Figure 2a</a>. Mortality: <a href="https://doi.org/10.1111/j.1728-4457.2007.00171.x" target="_blank" rel="noopener">Gurven & Kaplan, Table 2</a>. Growth target: <a href="https://doi.org/10.1098/rstb.2019.0708" target="_blank" rel="noopener">Tallavaara & Jørgensen, Table 1</a>.</p><p>The fertility shape comes from image extraction, not the original numeric table. Mortality is pooled across sexes. Equal newborn sex probabilities and year-end births are assumptions. Birth spacing, partner availability, food limits and ancient transfer are not modeled.</p><p>${data.checks.replicates} seeded runs checked against the expected population trajectory. Recovering a fitted growth target is an implementation check, not independent historical validation.</p></details><p><a id="growth-download" href="/reference/growth-run.csv" download>Download run (CSV)</a> · <a href="/reference/fertility-reference.json">Extraction & assumptions</a> · <a href="/reference/growth-checks.json">Checks</a></p></div></aside></div><p class="reference-boundary">This experiment connects births and deaths in the replacement engine. It does not estimate ancient population size, carrying capacity or arrival dates.</p>`;
  const el=(id:string)=>document.getElementById(id)!;const fmt=(n:number)=>Math.round(n).toLocaleString();
  function draw(){
   const rows=births?data.with_births:data.without_births,r=rows[year];
   const top=Math.ceil(Math.max(...data.with_births.map(r=>Math.max(r.people,r.expected_people)))/2000)*2000;
   const x=(n:number)=>70+n/160*700,y=(n:number)=>330-n/top*295;
   const line=(key:'people'|'expected_people')=>rows.map((r,i)=>`${i?'L':'M'}${x(r.year)},${y(r[key])}`).join(' ');
   el('growth-chart').innerHTML=`<title>${births?'Births and deaths':'Deaths only'}: 5,000 initial people</title>${[0,1,2,3,4].map(i=>{const n=i*top/4;return `<line x1="70" x2="770" y1="${y(n)}" y2="${y(n)}" stroke="#d0d3c9"/><text x="60" y="${y(n)+5}" text-anchor="end">${n/1000}k</text>`;}).join('')}<path d="${line('people')}" fill="none" stroke="#c02291" stroke-width="4"/><path d="${line('expected_people')}" fill="none" stroke="#20211f" stroke-width="1.5" stroke-dasharray="6 5"/><line x1="${x(year)}" x2="${x(year)}" y1="30" y2="335" stroke="#20211f"/><circle cx="${x(year)}" cy="${y(r.people)}" r="5" fill="#c02291"/>${[0,40,80,120,160].map(n=>`<text x="${x(n)}" y="355" text-anchor="middle">${n}</text>`).join('')}<text x="420" y="383" text-anchor="middle">Elapsed years</text>`;
   el('growth-year-label').textContent=`${year} years`;
   el('growth-readout').innerHTML=`<dl><div><dt>Living people</dt><dd>${fmt(r.people)}</dd></div><div><dt>Expected population</dt><dd>${fmt(r.expected_people)}</dd></div><div><dt>Births this year</dt><dd>${fmt(r.births)}</dd></div><div><dt>Deaths this year</dt><dd>${fmt(r.deaths)}</dd></div><div><dt>All births since start</dt><dd>${fmt(r.cumulative_births)}</dd></div><div><dt>All deaths since start</dt><dd>${fmt(r.cumulative_deaths)}</dd></div></dl><p class="cell-help">5,000 + ${fmt(r.cumulative_births)} − ${fmt(r.cumulative_deaths)} = ${fmt(r.people)} people.</p>`;
   (el('growth-download') as HTMLAnchorElement).href=births?'/reference/growth-run.csv':'/reference/no-births-run.csv';
  }
  el('growth-year').addEventListener('input',e=>{year=Number((e.target as HTMLInputElement).value);draw();});el('growth-comparison').addEventListener('change',e=>{births=(e.target as HTMLSelectElement).value==='births';draw();});draw();
 }catch(e){if(root.hidden||document.querySelector('[data-lab="growth"]')?.getAttribute('aria-pressed')!=='true')return;root.innerHTML='<p role="alert"></p>';root.firstElementChild!.textContent=e instanceof Error?e.message:'Could not load the reference.';}
}
